
version: "3.7"

services:
  evolution2ee8f16a-redis:
    image: redis:latest
    command: [
        "redis-server",
        "--appendonly",
        "yes",
        "--port",
        "6379"
      ]
    volumes:
      - evolution2ee8f16a-redis-data:/data
    networks: 
      - interna
    deploy:
      placement:
        constraints:
          - node.role == manager
     # resources:
      #  limits:
        #  cpus: "0.5"
        #  memory: 1024M

volumes:
  evolution2ee8f16a-redis-data:
    external: true
    name: evolution2ee8f16a-redis-data


networks: 
  interna: 
    external: true


